# Dependências de desenvolvimento e testes do SFSC.
# Instala também o runtime. Equivalente a: pip install -e .[dev]
-r requirements.txt
pytest~=8.0
pytest-cov~=5.0
ruff>=0.6
mypy>=1.10
types-PyYAML
pre-commit>=3.7
