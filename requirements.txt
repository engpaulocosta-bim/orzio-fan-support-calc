# Dependências de runtime do SFSC (aplicação Streamlit + motor de cálculo).
# Fonte canónica: pyproject.toml [project.dependencies]. Este ficheiro espelha-as
# com pins compatíveis (~=) para instalação reprodutível sem instalar o pacote.
# Desenvolvimento: requirements-dev.txt | Build desktop: requirements-build.txt
pydantic~=2.5
pyyaml~=6.0
numpy~=1.26
reportlab~=4.0
openpyxl~=3.1
pandas~=2.1
streamlit~=1.30
